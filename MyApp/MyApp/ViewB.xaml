<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyApp.ViewB"
             BackgroundColor="WhiteSmoke">
  <ContentView.Content>
        <StackLayout >
            <Label Text="Datails" HorizontalOptions="StartAndExpand" />
            <Label Text="{Binding Path=SelectedItemOnA.Name}" HorizontalOptions="StartAndExpand" />
            <Label Text="{Binding Path=SelectedItemOnA.Email}" HorizontalOptions="StartAndExpand" />
        </StackLayout>
    </ContentView.Content>
</ContentView>